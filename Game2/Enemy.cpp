#include "Enemy.h"

Enemy::Enemy() {
	
}

Enemy::~Enemy() {
	
}


void Enemy::update(float dt) {
	Enemy::Player::update(dt);

	//Need to fix control issues
	//	Find some way to replace Player update
}